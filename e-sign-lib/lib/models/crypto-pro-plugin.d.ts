export declare class CryptoProPluginInfo {
    pluginVersion: string;
    cspVersion: string;
    constructor({ cadesVersion, cspVersion }: {
        cadesVersion: string;
        cspVersion: string;
    });
}
